{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "the window and the panel talk to this app's own commands, and the window listens for the section the tray asks it to open",
  "windows": ["main", "popover"],
  "permissions": ["core:default", "core:event:allow-listen"]
}

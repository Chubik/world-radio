/target
/data
/_private
/.claude
r4dio.log
crates/r4dio-macos/gen/
.superpowers/

# secrets never belong in the repo
.env
.env.*

# macos leaves these everywhere
.DS_Store

# internal planning docs: this repository is PUBLIC. plans and specs describe
# our deploy layout, server users and internal service names — none of that is
# for a public tree. they live on disk and in the private mirror instead.
docs/superpowers/
docs/design/
